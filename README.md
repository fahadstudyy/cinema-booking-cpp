# Cinema Booking System (C++)

A console-based cinema ticket booking system written in C++, with bookings persisted to a local data file (`card.dat`).

## Stack

C++, file-based storage, compiled console application.

## Note

An early systems-programming exercise focused on file I/O and basic booking logic in C++.
